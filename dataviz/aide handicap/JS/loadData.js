async function loadData(resquest){
    const response  = await fetch(resquest);
    const rawData   = await response.json();

    return rawData;
}

async function loadData2(resquest){
    const response  = await fetch(resquest);
    const rawData   = await response.json();

    return rawData;
}

async function loadData3(resquest){
    const response  = await fetch(resquest);
    const rawData   = await response.json();

    return rawData;
}

async function loadData4(resquest){
    const response  = await fetch(resquest);
    const rawData   = await response.json();

    return rawData;
}